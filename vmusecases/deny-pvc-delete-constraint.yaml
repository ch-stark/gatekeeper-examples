apiVersion: constraints.gatekeeper.sh/v1beta1
kind: DenyPVCDeleteIfAttached
metadata:
  name: deny-pvc-delete-if-attached
spec:
  match:
    kinds:
      - apiGroups: [""]
        kinds: ["PersistentVolumeClaim"]
